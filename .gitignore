# ── Build output ──────────────────────────────────────────────────────────────
bin/
obj/
publish/
Releases/
Setup/Output/

# ── User config & data (contains Jira credentials — never commit) ─────────────
%AppData%/Trakr/
config.json
sessions.csv
tracker.log

# ── Visual Studio / Rider ─────────────────────────────────────────────────────
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
*.rsuser

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── NuGet ─────────────────────────────────────────────────────────────────────
*.nupkg
packages/

# ── Dev scripts (internal, not for repo) ─────────────────────────────────────
build.ps1

# ── Secrets (extra safety net) ────────────────────────────────────────────────
*.env
secrets.json
appsettings.*.json
