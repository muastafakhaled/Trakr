<Window x:Class="JiraTimeTracker.Windows.StopPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="#0d1117" Foreground="#e6edf3" FontFamily="Segoe UI" FontSize="13"
        ResizeMode="NoResize" WindowStartupLocation="Manual"
        Title="Still working?" Width="380" Height="210"
        Topmost="True">

  <Window.Triggers>
    <EventTrigger RoutedEvent="Window.Loaded">
      <BeginStoryboard>
        <Storyboard>
          <DoubleAnimation Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
      </BeginStoryboard>
    </EventTrigger>
  </Window.Triggers>

  <Grid x:Name="Root" Margin="24" Opacity="0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- IDE label -->
    <TextBlock Grid.Row="0" x:Name="IdeLabel" Text="IDE closed"
               FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource YellowBrush}"
               Margin="0,0,0,8"/>

    <!-- Question -->
    <StackPanel Grid.Row="1" Margin="0,0,0,16">
      <TextBlock x:Name="QuestionLabel" Text="Still working?"
                 FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"
                 Margin="0,0,0,4"/>
      <TextBlock x:Name="DetailLabel" Text=""
                 Foreground="{StaticResource SubtextBrush}" FontSize="12"/>
    </StackPanel>

    <!-- Buttons -->
    <Grid Grid.Row="3">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="8"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Button Grid.Column="0" Content="⏹  Stop &amp; Log"
              Style="{StaticResource DangerBtn}"
              BorderBrush="{StaticResource RedBrush}" BorderThickness="1"
              Click="Stop_Click" Padding="12,10"/>
      <Button Grid.Column="2" Content="Keep Running"
              Style="{StaticResource GhostBtn}"
              Click="Keep_Click" Padding="12,10"/>
    </Grid>
  </Grid>
</Window>
